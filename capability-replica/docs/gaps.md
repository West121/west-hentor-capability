# Replica Parity Gaps

This file tracks what has been copied from the decompiled Capability Table system and what still needs work. Data remains anonymized and local.

## Implemented In This Replica

- React + Ant Design admin shell with SGS logo, login, permission-gated menu, protected routes, and a responsive collapsed sidebar for narrow viewports.
- Spring Boot ABP-style response envelope with copied `/api/services/app/...` route names.
- Ability management: `GetMyOrgSetting` organization tabs, original `Instruction` ability-description display/edit block with `Pages.AbilityManagement.EditDesc` gating, TipTap rich-text editing for copied UEditor-style HTML descriptions, UEditor-compatible image upload output plus inline image read URL, safe rendering of copied rich HTML, current-organization list/search/import scope, organization `propertyList` driven table columns and edit fields, original `[Description]`-attributed `AbilityTable` property list/order, original `AbilityPropertyDto.Name`/`CamelCase` field setting shape with PascalCase save compatibility, original `skipCount/maxResultCount` server-side pagination, original blank `Sorting` normalization to `Id` plus explicit sorting for management/query ability lists, original dynamic filter payload compatibility, lab/qualification filtering (`labAbility` + `ability`), list, search, create, edit, delete, original action dropdown, row-scoped history modal, row `isCollected` state, favorite/unfavorite toggle, original `DeleteAll` by organization name, `GetOrgTypeLit`, tabbed create/edit form, business-line driven type autocomplete, save-time ability organization/type reference normalization, copied create/update required-field validation, duplicate ability rejection, original two-row Excel import template with merged laboratory-capability header, current-business-line `orgId`/`propertyName` driven template columns including original PascalCase `PropertyName` input, stable laboratory column order, `ALL/CNAS/CMA/√` lab values, merged-cell and formula-cell display-value reading for imported rows, original two-row ability export headers, original bold 12pt Excel header styling, original default column widths for ability data exports, org-scoped ability export file/sheet names, `CHEM/NF/Lab Group` special ability export columns, original `CMA,CNAS` lab ability export values, filtered `ExportData` request bodies, ability-list export button scoped to the active business line/search filters, Excel download/export, original-style import preview instructions, separate save-new and save-new-plus-duplicate import actions, row-numbered import rows, duplicate detection, copied required-field validation for the original ability import layout, lab-column value/unknown-lab validation, dynamic lab preview columns, downloadable Excel error reports including lab columns, and confirmed import save.
- Ability Excel file-name verification now covers the original `.NET DateTime.Now.ToFileTime()`-style 18-digit suffix for org-scoped ability templates and exports; ability pagination verification now covers original `FindPageAblibitiesInput` and `FindAblibitiesInput` blank sorting normalization to `Id` plus explicit `testItem ASC` sorting, and subcontract `FindList` verification now covers original blank sorting normalization to `Id` plus explicit `labName DESC` sorting.
- Ability-description HTML cleanup now strips common UEditor Word-paste artifacts, including Office conditional comments, `Mso*` classes/styles, namespaced Word tags such as `o:p`, and `w:WordDocument` fragments, while preserving normal paragraphs, tables, images, links, emphasis, safe table layout attributes (`width`, `height`, `border`, `cellpadding`, `cellspacing`, `colspan`, `rowspan`), and safe UEditor inline styles for `text-align`, text color, and background color; safe Word/VML `<v:shape>` image fragments are converted back into regular `<img>` tags with copied width, height, and title metadata, `MsoHeading` paragraphs become bold title paragraphs, and local `file://` Word images are kept as pending placeholders with the original `word_img` marker that the editor can batch replace through the copied UEditor image upload endpoint.
- Ability-description editing now includes common UEditor-style toolbar actions through TipTap + Ant Design: undo, redo, paragraph/heading selection, text alignment, text color, background color, quote, horizontal rule, link, image upload, Word image replacement, and clear formatting.
- Original generated HTTP DELETE query contracts: copied for `Ability/DeleteAbility`, `Ability/DeleteAll`, `Laboratory/DeleteLab`, `SampleType/DeleteSampleType`, `Sample/DeleteSample`, `DashboardCustomization/DeletePage`, `MyFavorite/DeleteMyFavorite`, `MyFavorite/RemoveItem`, `OrganizationUnit/DeleteOrganizationUnit`, `OrganizationUnit/RemoveUserFromOrganizationUnit`, `OrganizationUnit/RemoveRoleFromOrganizationUnit`, `Role/DeleteRole`, `User/DeleteUser`, `UserDelegation/RemoveDelegation`, `Language/DeleteLanguage`, `Notification/DeleteNotification`, `Notification/DeleteAllUserNotifications`, `Tenant/DeleteTenant`, `Edition/DeleteEdition`, and the four dynamic parameter delete routes, with original query parameter names and POST compatibility retained.
- Original generated business GET query contracts: copied for ability edit/type/template reads, lab edit reads, sample type edit/list-by-org reads, and sample edit/list-by-type reads, including original `Id`, `OrgId`, and `TypeId` query parameter names.
- Original generated administration GET query contracts: copied for role list/edit, user list/export/edit/permission-edit, language edit, tenant list/edit/features, and edition edit/tenant-count reads, including repeated `Permissions` query values, original `Id`, `editionId`, pagination, sorting, blank user-list sorting normalization to `Name`, blank user-export sorting normalization to `Name,Surname`, blank tenant-list sorting normalization to `TenancyName`, tenant `editionDisplayName` sorting alias, lockout, edition filter, and date-range query parameter names where the decompiled Angular proxy emits them.
- Original generated organization unit membership GET query contracts: copied for `GetOrganizationUnitUsers` and `GetOrganizationUnitRoles`, including original `Id`, pagination, blank sorting defaults, `userName`/`displayName` nested-field aliases, and `addedTime` membership-time alias.
- Original generated language text GET query contract: copied for `Language/GetLanguageTexts`, including original `MaxResultCount`, `SkipCount`, `Sorting`, `SourceName`, `BaseLanguageName`, `TargetLanguageName`, `TargetValueFilter`, and `FilterText` query parameter names.
- Original generated ability-property and edition-combobox GET query contracts: copied for `AbilityProperty/GetOrgAbilitySetting` and `Edition/GetEditionComboboxItems`, including original `OrgId`, `selectedEditionId`, `addAllItem`, and `onlyFreeItems` query parameter names.
- Original generated POST-without-body read contracts: copied for `Account/BackToImpersonator`, `Ability/FindAllAblibities`, `AbilityProperty/AbilityPropertyList`, `Dashboard/Statistics`, `Dashboard/OrgCount`, `Dashboard/ChangeCountInWeek`, `Install/CheckDatabase`, `Laboratory/List`, and `WebLog/DownloadWebLogs`; frontend calls these routes with POST and no explicit request body, and `BackToImpersonator` returns the impersonator tenant stored in the current token or preserved through a linked-account switch token when present while rejecting stale delegated sessions with the original active-delegation error.
- Original generated audit GET query contracts: copied for audit log list/export, entity change list/export, entity type changes, and entity property changes, including original `StartDate`, `EndDate`, `UserName`, `ServiceName`, `MethodName`, `BrowserInfo`, duration, exception, `EntityTypeFullName`, `EntityId`, pagination/sorting, and lowercase `entityChangeId` query parameter names; paged audit reads/exports enforce the original `PagedAndSortedInputDto` range.
- Original generated dashboard customization GET query contracts: copied for user dashboard layout, dashboard definition, widget definitions, and dashboard customization setting-name reads, including original `DashboardName`, `Application`, and lowercase `application` query parameter names.
- Original generated Host/Tenant dashboard GET query contracts: copied for host top stats, host income statistics, host edition-tenant statistics, tenant dashboard data, and tenant sales summary, including original `StartDate`, `EndDate`, `IncomeStatisticsDateInterval`, and `SalesSummaryDatePeriod` query parameter names plus Angular `Date.toJSON()` timestamp parsing.
- Original generated ability history GET query contracts: copied for ability history list and detail reads, including original `Sorting`, `MaxResultCount`, `SkipCount`, and `Id` query parameter names, original blank sorting normalization to `ChangeTime Desc`, explicit sorting such as `changeTime ASC`, plus original `PagedAndSortedInputDto` range validation.
- Original generated organization unit membership GET query contracts: copied for organization unit user and role paging reads, including original `Id`, `Sorting`, `MaxResultCount`, and `SkipCount` query parameter names plus original positive `Id`, non-negative `SkipCount`, and 1-1000 `MaxResultCount` validation with matching frontend required-id typing.
- Original generated account relation GET query contracts: copied for user delegation and linked-user paging reads, including original `MaxResultCount`, `SkipCount`, and `Sorting` query parameter names, original user-delegation `userName ASC` sorting normalization, plus original `PagedAndSortedInputDto` range validation.
- Original generated profile picture GET query contracts: copied for profile-picture-by-id and friend-profile-picture reads, including original lowercase `profilePictureId` and PascalCase `ProfilePictureId`, `UserId`, and `TenantId` query parameter names; profile-picture-by-id also keeps the original anonymous access behavior.
- Original generated common lookup and timing GET query contracts: copied for edition combobox and timezone reads, including original lowercase `onlyFreeItems` plus PascalCase `DefaultTimezoneScope` and `SelectedTimezoneId` query parameter names; `CommonLookup/FindUsers` also enforces the original `PagedAndFilteredInputDto` paging range.
- Original generated Demo UI helper query contracts: copied for date, date-time, date-range, and value POST-query echo routes plus country lookup GET-query, including original lowercase `date`, `startDate`, `endDate`, `input`, and `searchTerm` parameter names; `DemoUiComponents/UploadFiles` stores uploads as binary objects and returns original GUID-style ids.
- Original generated payment GET query contracts: copied for `Payment/GetPaymentHistory`, including original paging keys, blank sorting normalization to `CreationTime`, and `editionDisplayName` sorting alias.
- Original generated payment GET/query contracts: copied for payment info, payment history, active gateways, payment detail, last-completed payment, completed-payment existence checks, Stripe payment detail/result reads, payment status callbacks, free-edition switching, and PayPal confirmation, including original `UpgradeEditionId`, `Sorting`, `MaxResultCount`, `SkipCount`, `RecurringPaymentsEnabled`, lowercase `paymentId`, `paypalOrderId`, `upgradeEditionId`, `editionId`, `StripeSessionId`, and `PaymentId` query parameter names.
- Original generated Webhook query contracts: copied for subscription detail/filter reads, send-attempt paging/event reads, `IsSubscribed` POST-query, and `Resend` POST-query, including original `subscriptionId`, `webhookName`, `SubscriptionId`, `MaxResultCount`, `SkipCount`, `Id`, and `sendAttemptId` query parameter names; send-attempt paging enforces the original `PagedInputDto` range.
- Original generated detail/selection GET query contracts: copied for favorite ability list/edit detail, webhook event detail, invoice info, and tenant-registration edition detail, including original `MyFavoriteId`, `Id`, lowercase `id`, and lowercase `editionId` query parameter names; tenant-registration edition detail keeps the original empty `additionalData` dictionary, and invoice info also returns the original ownership error when the payment tenant differs from the current session tenant.
- Original generated notification/chat query and update contracts: copied for `Notification/GetUserNotifications` GET-query, `Notification/DeleteAllUserNotifications` DELETE-query, `Notification/UpdateNotificationSettings` HTTP `PUT`, and `Chat/GetUserChatMessages` GET-query, including original `State`, `StartDate`, `EndDate`, `MaxResultCount`, `SkipCount`, `TenantId`, `UserId`, and `MinMessageId` query parameter names; notification list totals, unread counts, and bulk deletion apply the original date range filters, notification list paging enforces the original `PagedInputDto` range, notification settings keep the original `receiveNotifications` plus `notifications` DTO shape without local channel toggles, `SetNotificationAsRead` and `DeleteNotification` return the original ownership errors for another user's notification id, message history enforces the original positive `UserId` DTO range with matching frontend required-id typing, `MinMessageId` uses the original older-message cursor behavior (`Id < MinMessageId`, latest 50, then ascending display order), missing-target chat sends return `Target user could not be found. It's probably deleted.`, sender-blocked-target chat sends return `User is blocked.`, and receiver-blocked-sender sends stay successful while skipping the receiver-side message copy.
- Original generated dynamic-parameter read contracts: copied for single definition/value/entity-value GET reads, allowed-input-type POST-query, dynamic-parameter value reads, entity-parameter reads, and entity-parameter-value reads, including original lowercase `id`, lowercase `name`, `Id`, `EntityFullName`, `EntityId`, and `ParameterId` query parameter names.
- Original generated settings/UI method contracts: copied for Host/Tenant settings updates and UI customization updates as HTTP `PUT`, plus `ChangeThemeWithDefaultValues` as POST-query with original lowercase `themeName`.
- Original generated HTTP `PUT` update contracts: copied for `Ability/UpdateAbility`, `OrganizationUnit/UpdateOrganizationUnit`, `User/UpdateUserPermissions`, `Profile/UpdateCurrentUserProfile`, `Profile/UpdateProfilePicture`, `Profile/UpdateGoogleAuthenticatorKey`, `Session/UpdateUserSignInToken`, `Language/UpdateLanguageText`, `DynamicParameter/Update`, `DynamicParameterValue/Update`, `EntityDynamicParameter/Update`, `EntityDynamicParameterValue/Update`, `WebhookSubscription/UpdateSubscription`, `Tenant/UpdateTenant`, `Tenant/UpdateTenantFeatures`, and `Edition/UpdateEdition`; frontend calls these routes with `apiPut` and backend POST compatibility remains.
- Original generated remaining route entry points: copied frontend API exposure for `Ability/GetAllUnits`, `Account/ResolveTenantId`, `Account/DelegatedImpersonate`, `OrganizationUnit/MoveOrganizationUnit`, and `TokenAuth/TestNotification`; `MoveOrganizationUnit` enforces the original positive `Id` validation with matching frontend required-id typing, `ResolveTenantId` includes the original blank-`c` current-session tenant branch, `Account/Impersonate` and `Account/SwitchToLinkedAccount` enforce original positive user-id validation with matching frontend required-id typing, `Account/DelegatedImpersonate` preserves the tenant stored on the selected user delegation, delegated session validation mirrors the original active-delegation check, `TestNotification` accepts original GET query parameters `message` and `severity` and creates a local unread `App.SimpleMessage` notification, and TokenAuth now keeps the original generated `LogOut` GET plus `ImpersonatedAuthenticate` / `LinkedAccountAuthenticate` POST-query contracts.
- Original GraphQL backend entry points: copied `/graphql` and `/ui/playground`; `/graphql` covers the decompiled query container fields `roles`, `organizationUnits`, and `users`, including original argument names, standard POST JSON and GET query-string transport with `variables` and `operationName`, GraphQL-style `data/errors`, recursive selection-set field trimming, top-level and nested field aliases, minimal `__schema` / `__type` introspection, field-level permission checks, original `[ERR001]` no-permission message, and `users.items.roles` / `users.items.organizationUnits` detail output that only requires the user-query permission.
- Original generated frontend route aliases: copied `/passport/login`, `/ablibity/subcontract-ability`, `/sys/auditlogs`, and `/exception/403|404|500|trigger`; Web.Core MVC error routes `/Error`, `/Error/Index`, `/Error/E403`, and `/Error/E404` now hand off to the copied exception pages; the menu now uses the original `app-data.json` subcontract path and audit-log path `/logs/audit_log`, while old replica aliases remain routed.
- Original NG-Alain template/demo frontend routes: copied the `widgets`, `style`, `extras`, `pro`, and `delon` route groups into `templateRoutes.ts`, including deep links such as `/pro/form/basic-form`, `/pro/account/center/articles`, `/pro/account/settings/security`, `/delon/st`, `/delon/guard/admin`, and `/style/colors`; these routes render through a shared React + Ant Design template page instead of falling through to 404, and `/pro/account/center` plus `/pro/account/settings` keep the original default child redirects to `articles` and `base`.
- Ability create/edit now covers the core decompiled `CreateAbilityInput` fields: product/test item notes, method notes, standard-number families, cycle/test-time/sample requirements, price notes, remarks, and per-lab ability/CMA/CNAS toggles; the modal watches the selected business line and hides fields not enabled in that organization's `propertyList`.
- Ability query: search table with the original query fields and copied business columns, original `skipCount/maxResultCount` server-side pagination, client-visible `Ability.Description` HTML display from Session settings, original `FindAblibities` response shape with `labs` dropdown data, original dynamic filter payload compatibility, lab/qualification filtering with `ALL` rows matching `CNAS` and `CMA` qualification filters, query rows isolated from management-page favorite state, original text-style laboratory ability display, and row-scoped `FindHistory` ability history modal.
- Dashboard: `Statistics`, `OrgCount`, and `ChangeCountInWeek` endpoints with frontend cards.
- Host/Tenant dashboards: `HostDashboardAppService` top stats including original fixed `DashboardPlaceholder1=125` and `DashboardPlaceholder2=830`, original status `2` payment filtering for host subscription/income totals, recent tenants, expiring tenants with original final remaining-day/tenant-name ordering and rounded-total-days remaining count, income statistics with original `IncomeStastistic.Date` date-time output, `Label` short-date formatting, zh-Hans week-start splitting, calendar-month grouping, and edition tenant statistics that exclude tenants without an assigned edition, `TenantDashboardAppService` top stats, dashboard data, sales summary, daily sales, profit share, member activity, regional stats, general stats, and original `DashboardRandomDataGenerator` labels/ranges, permission migrations, and React dashboard pages.
- Dashboard customization: `DashboardCustomizationAppService` user dashboard, save page, rename page, add page, add widget, delete page, dashboard definition, and widget definition routes with persisted local Host/Tenant layouts.
- Favorites: `GetMyFavoriteList` with the original virtual `默认清单` item and original `MyFavoriteDto` `id/name` shape, current-user scoped custom/default lists, `GetMyFavoriteAbilityList` including default `MyFavoriteId:null` item lookup, `SaveOrUpdateMyFavorite` with original duplicate-name guard, `AddItem` single-list move semantics, `RemoveItem`, `DeleteMyFavorite`, and ability-list `isCollected` status.
- Subcontract ability: `FindList`, original `skipCount/maxResultCount` server-side pagination, original four-field keyword filter (`LabName`, `ContactDetails`, `TestCategory`, `CmaOrCnas`), original blank `Sorting` normalization to `Id` plus explicit sorting such as `labName DESC`, original-compatible upload reader with fixed second-row headers, third-row data start, formula display values, local two-row template helper, MVC upload parsing, original immediate upload-save UI flow, duplicate/error detection, downloadable Excel error reports, original `SaveExcelData` replacement behavior by imported `LabName` while ignoring `OnlySaveNew`, and list/search UI.
- Lab management with `GetLabForEdit`, `LaboratoryDto.Code`/`Name` required validation and matching React form/type constraints, original-compatible `/business/sampleType` and `/business/sample` sample CRUD routes, original duplicate validation messages for laboratory code, sample type name, and sample name (`<value>已存在`), and organization ability property settings.
- Organization unit management: tree CRUD with ABP-style `code/memberCount/roleCount`, original create/update `DisplayName` required plus 128-character validation, update/move `Id` positive-range validation, matching React name input cap, organization users/roles, `FindUsers`, `FindRoles`, original lookup pagination validation, and add/remove users/roles with original positive organization/member/role id validation.
- User and role management: list/search/create/edit/delete flows, role assignments, organization assignments, lab assignments, original `UserList.xlsx` Excel export name/sheet/columns/blank second row/third-row data start/creation-date format, copied `UsersControllerBase.ImportFromExcel` route with first-file multipart behavior, `UserName/Name/Surname/EmailAddress/PhoneNumber/Password/Roles` parsing, and downloadable `InvalidUserImportList.xlsx` using the original `Invalid user imports` sheet, headers, and row-2 data start, original `App.DownloadInvalidImportUsers` data notification, original `App.SimpleMessage` success notification, `GetUserForEdit` new-user defaults including password and security toggles, original `UserEditDto` string-length validation including the optional `Surname` 64-character cap, original `UserRoleDto` role edit output with `isAssigned` defaults, original `CreateOrUpdateUser` void/null response while persisting user assignments, original current-user delete guard, original positive-id and required permission-list validation for `UpdateUserPermissions`, original `GetRoleForEdit` non-null new-role DTO and `FlatPermissionDto` fields sorted by display name, original `CreateOrUpdateRole` void/null response plus required `Role`/`GrantedPermissionNames`/`Role.DisplayName` validation while persisting role changes, user-specific permission tree editing/reset, `PermissionAppService.GetAllPermissions` access without requiring role-management permission, user unlock, password reset, and role permission tree editing.
- User import invalid-file handling and reader parity: workbooks that cannot be converted or contain no user rows publish the original `App.SimpleMessage` warning text `User import process has failed. File is invalid.` instead of an invalid-user error-report download, the React users page shows that warning state instead of a false success summary, and the importer reads fixed user columns 0-6 across all worksheets while treating blank first-column rows as empty like the original NPOI importer.
- Local role-based authorization: login issues per-user tokens, session returns role-derived permissions including ability create/edit/public-edit/delete/delete-all/import/history/edit-description points, ability description editing uses `Pages.AbilityManagement.EditDesc`, permission tree lookup only requires a signed-in user like the original app service route, backend routes reject anonymous/unauthorized calls, local mock access tokens are no longer accepted as admin sessions, and the frontend menu/action controls follow those permissions.
- Session: `SessionAppService` `GetCurrentLoginInformations` returns user, token-derived tenant, application, active theme, local permissions, and client-visible settings including `Ability.Description`, and `UpdateUserSignInToken` returns sign-in token plus encoded user/tenant ids from the same tenant context.
- Account/TokenAuth/UserLink/UserLogin security: signed JWT access tokens with tenant context, refresh tokens that preserve tenant context, `TokenAuth/Authenticate` requiring the original Base64-encoded `password` payload and rejecting plaintext/non-Base64 password payloads, frontend login sending `userNameOrEmailAddress` plus Base64 password, blank login names/passwords rejected instead of defaulting to local demo credentials, original `shouldResetPassword/passwordResetCode/userId/returnUrl` challenge for users flagged to change password before token issuance, normal and external `singleSignIn` return URLs using original `accessToken=<signInToken>&userId=...&tenantId=...` parameters, logout with the original generated GET route, local two-factor pending-challenge guard with random 6-digit code delivery, the original void/null `SendTwoFactorAuthCode` success response, `SendSecurityCodeErrorMessage` when no pending challenge exists, `InvalidSecurityCode` when a two-factor login code is wrong, and remember-client tokens only after successful two-factor code verification, external authentication provider list sourced from configured provider settings without local fallback providers and with original Google `AdditionalParams.UserInfoEndpoint`, local `ExternalAuthenticate` access/refresh token exchange, tenant availability/resolve checks including blank `ResolveTenantId.c` returning the current session tenant, inactive `IsTenantAvailable` responses omitting `tenantId`, and active `IsTenantAvailable` server-root formatting sourced from `app.server-root-address` with `{TENANCY_NAME}` replacement only when configured, registration with original tenant self-registration/default-active/email-confirmation/captcha-empty setting behavior using the token tenant when present, password reset and email activation including the original unknown-email `InvalidEmailAddress` check before sending codes, ABP `SimpleStringCipher` encrypted `c` payload resolution, and strict stored-code matching without a local fixed-code bypass, impersonated authenticate carrying target tenant context through the original POST-query route, delegated impersonation including the original POST route and saved delegation tenant context, delegated JWT/session validation that allows impersonation permission or requires an active user delegation and otherwise returns `ThereIsNoActiveUserDelegationBetweenYourUserAndCurrentUser`, back-to-impersonator tokens returning to the current token's impersonator user/tenant, linked-account authenticate preserving impersonator claims when switching from an impersonated session through the original POST-query route, linked account list/recent/link/unlink routes with original link username/password required validation, same-account, must-change-password, and no-linked-account guards, recent login attempt route, persisted login attempts, and a React account security console.
- Account center: local password verification/change password with the original `Incorrect password.` current-password failure and current-tenant password-complexity validation for length, digit, lowercase, uppercase, and non-alphanumeric rules, `ProfileAppService` profile edit, profile picture including anonymous profile-picture-by-id reads, required user-language changes, anonymous password-complexity read from the current tenant settings, Google Authenticator key using the original lowercase GUID-derived 10-character shape, SMS verification with random 6-digit stored codes and phone-number changes deferred until code verification succeeds, collected-data zip generation with `ProfileInfo.txt`, `ProfilePicture.png`, and Chat message Excel exports with original `Chat_._<user>.xlsx` host-conversation names plus tenant `tenancyName` conversation names, blank second row, third-row data start, and date-time cells, `App.GdprDataPrepared` notification data, and notification-triggered download routes, plus `UserDelegationAppService` delegated user list/create/remove/active routes with the original self-delegation, source-user-only removal, and `CreateUserDelegationDto` validation guards and a React profile page covering profile/avatar, password, security verification, and delegations with a required delegation date range.
- Language management: `LanguageAppService` language list/edit/default/delete routes, original `GetLanguages` display-name sorting, `GetLanguageForEdit` language-name and FamFamFam flag comboboxes with selected states, original culture-derived language display name and `IsEnabled` save semantics, language text list/update routes, original `CreateOrUpdateLanguage` and `UpdateLanguageText` void/null write responses, duplicate-language guard, tenant-side create-language guard, copied language DTO required-field/string-length validation, migrated language permissions, and a React language/text maintenance page with matching language-name/icon/key field caps and Ant Design selects for the edit comboboxes.
- Notification center: `NotificationAppService` user notification list/read/delete/settings routes, typed notification data payloads, GDPR collected-data download button, invalid-user import report download button, persisted original `receiveNotifications` and subscription settings with `NotificationSubscriptionDto.Name` required/max-96 validation, header unread badge, and a React notification center page with matching hidden-name input cap.
- Cache management: `CachingAppService` cache list with original name-only `CacheDto` shape, single-clear/all-clear routes, `Pages.Administration.Host.Maintenance` permission migration, a React cache maintenance page, and automatic expansion of the active nested menu group.
- Dynamic parameters: `DynamicParameterAppService`, `DynamicParameterValueAppService`, `EntityDynamicParameterAppService`, `EntityDynamicParameterValueAppService`, and `DynamicEntityParameterDefinitionAppService` routes for CRUD/bulk/clean flows, original `DynamicParameter`, `DynamicParameterValue`, `EntityDynamicParameter`, and `EntityDynamicParameterValue` Add/Update void/null write responses, original `DELETE /Delete?id=...` delete routes for those four services, `FindAllowedInputType`, allowed input type lookup, entity lookup, permission migrations, persisted seed data, and a React tabbed management page.
- Host/Tenant settings: `HostSettingsAppService` and `TenantSettingsAppService` setting read/update routes with original update void/null responses, token-tenant `TenantSettings/GetAllSettings` and `UpdateAllSettings` isolation, ability settings with `EditDesc` permission gating, session-timeout provider defaults and minimum validation, test email simulation with original required/max-256 email validation, token-tenant logo/CSS clear routes, `TenantCustomizationController` logo/CSS upload with first-file multipart behavior plus session/explicit-tenant read routes, permission migrations, persisted settings, and a React tabbed settings page with tenant-aware brand preview URLs, matching session-timeout numeric minimums, and matching test-email input caps.
- UI customization: `UiCustomizationSettingsAppService` routes for listing themes, changing themes with default values, updating current/default theme settings, restoring system defaults, permission migration, persisted theme settings, and a React settings page.
- Webhooks: `WebhookSubscriptionAppService`, `WebhookSendAttemptAppService`, and `WebhookEventAppService` routes for available events, subscriptions with original Add/Update void/null responses, activation, test publishing, send attempts, event detail, and resend, plus permission migrations and a React Webhook management page.
- Chat/Friendship: `ChatAppService` and `FriendshipAppService` routes for friends, message history with original positive `UserId` validation and `MinMessageId` older-message paging, mark-read, friendship request by user id/name with original positive user-id validation, duplicate-friend guard, and self-friend `You can not be a friend with yourself.` guard, username-request `tenancyName` validation before user lookup, original `There is no tenant defined with name ...` failure for unknown tenants and missing users, accept/block/unblock positive user-id validation plus missing-friendship `Friendship does not exist between ...` guard, accept, block, unblock, missing-target `Target user could not be found. It's probably deleted.` guard, sender-blocked `User is blocked.` guard, receiver-blocked delivery suppression, persisted chat data, unread counts, original GDPR Chat message Excel export shape including host-conversation `.` naming and tenant `tenancyName` naming, blank second row and third-row data start, and a React chat page with matching required user-id API typing. The local REST `Chat/SendMessage` route emulates the original SignalR send path for this replica.
- Tenant/Edition/Subscription/Invoice: `TenantAppService`, `EditionAppService`, `PaymentAppService`, `InvoiceAppService`, `SubscriptionAppService`, `StripePaymentAppService`, and `PayPalPaymentAppService` routes for tenant CRUD with original Create/Update void/null responses, tenant feature values with original positive-id and required-list validation, edition CRUD with original Create/Update void/null responses, original edition wrapper `Edition`/`FeatureValues` and `DisplayName` required-field validation, original `MoveTenantsToAnotherEditionDto` positive id range validation, original four-price edition free/paid classification, tenant counts, active gateways, current-token tenant payment info/history/last-completed/has-any/creation/status/free-switch/low-amount-upgrade changes, PayPal/Stripe `SetAsPaid()` confirmation, success-callback `Paid(2)` guard before `Completed(5)`, `SetAsCancelled()` cancellation guard, tenant-scoped recurring-payment toggles with `SubscriptionPaymentType` state, local Stripe sessions, Web.Host `POST /Stripe/WebHooks` entry point with unsigned callback rejection, local PayPal confirmations, invoice generation, permission migrations, persisted seed data, and a React tabbed management page.
- Tenant registration: `TenantRegistrationAppService` `RegisterTenant`, `GetEditionsForSelect`, and `GetEdition` routes with local edition feature output, original empty `EditionSelectDto.additionalData`, original subscription-start guards for no-edition/free/trial/paid starts, host self-registration/default-active/email-confirmation/captcha-empty setting behavior, original `RegisterTenantInput` required-field and string-length validation, logged-in upgrade edition filtering by current edition and last completed payment period, trial/paid activation behavior, duplicate tenancy validation, and frontend API types with original required registration fields.
- Standard update: Excel upload parses the original `原标准号/新标准号/标准名称/标准状态/备注` row DTO as `old/new/name/statu/remark`, returns the original MVC `items/file` response shape without local match/error fields, and `UploadNewStandard` directly applies the copied StandardNo replacement behavior while copying the uploaded source file into the local History folder with the original `currentUser$DateTime.Now.ToFileTime()$fileName` pattern.
- Audit logs/entity history: `AuditLogAppService` `GetAuditLogs`, `GetAuditLogsToExcel`, `GetEntityHistoryObjectTypes`, `GetEntityChanges`, `GetEntityTypeChanges`, `GetEntityChangesToExcel`, and `GetEntityPropertyChanges` routes, `AbilityHistoryAppService.GetAbilityHistory` plus `GetHistoryDetail`, persisted local audit/entity/property change rows, original `AuditLogs.xlsx`/`DetailedLogs.xlsx` export names, original export headers/column order, blank second row, third-row data start, Excel date-time cell formatting, original audit-log `Parameters`/`Error state` default-width columns, original NPOI-style plain worksheets without extra freeze panes/table auto-filters, React ability-history detail modals, and a React tabbed audit console.
- Web log maintenance: `WebLogAppService` `GetLatestWebLogs` including original missing-log-folder empty output, `DownloadWebLogs` route, local Web log file generation for downloads, `WebSiteLogs.zip` download, and a React Web log maintenance page.
- Web Host MVC routes: `Home/Index`, `Ui/Index`, `Ui/Login` GET/POST, `Ui/Logout`, `AntiForgery/GetToken`, `api/AbpUserConfiguration/GetAll`, `Consent/Index` GET/POST, `Profile/UploadProfilePicture`, anonymous `Profile/GetDefaultProfilePicture`, `Chat/UploadFile`, `Chat/GetUploadedObject`, and `File/DownloadBinaryFile` with local temp/binary storage; `GET /Ui/Login?returnUrl=...` preserves the return URL for the React login handoff, and `POST /Ui/Login` keeps the original form-login redirect behavior for submitted `returnUrl`; the local `ConsentController` copy builds the original consent view-model shape from authorization `returnUrl` scopes, accepts original form names (`Button`, `ScopesConsented`, `RememberConsent`, `ReturnUrl`), redirects accepted/denied consent to `ReturnUrl`, and returns the original missing-scope validation message; MVC upload routes now read the first multipart file like the original `Request.Form.Files.First()` implementation, so the form field name is not required to be `file`; `File/DownloadTempFile` keeps the original `FileDto` required `FileName`/`FileToken` validation, optional `FileType`, and request `FileName` response header behavior, while `File/DownloadTempFile` and `File/DownloadBinaryFile` keep the original HTTP 404 response for missing temp-file tokens or binary object ids.
- Common lookup: `CommonLookupAppService` `GetEditionsForCombobox`, `FindUsers` with original user-name/name/surname/email filtering, original Name/Surname ordering, and original no-phone-number matching behavior, and `GetDefaultEditionName` routes backed by local edition/user data.
- Timing: `TimingAppService` `GetTimezones` and `GetTimezoneComboboxItems` routes with ABP list/combobox shapes and local Windows timezone mappings.
- Demo UI components: `DemoUiComponentsAppService` date/date-time/date-range echo, country lookup, selected-country echo, value echo, MVC `DemoUiComponentsController.UploadFiles` with copied 1MB limit/error behavior and BinaryObject-style GUID ids downloadable through `File/DownloadBinaryFile`, `Pages.DemoUiComponents` permission migration, and a React demo components page.
- Install: `InstallAppService` `Setup`, `GetAppSettingsJson`, and `CheckDatabase` routes with local appsettings snapshot, original second-setup guard, original fixed initial-language list for appsettings JSON, original single `WebSiteRootAddress` mode when server URL is blank, default-language update, Host SMTP/Billing propagation, and admin password reset behavior.
- FileController `DownloadTempFile` route with local temp file cache for template/export downloads.
- SQL Server persistence for copied master/business/admin data under the `sgsmineralscapability` database.
- Ability history page.

## Remaining Business Gaps

- Production-exact Excel validation beyond the copied org-scoped original-template/export-file-name/export-sheet-name/export-header/header-font/default-column-width/special-column/lab-value/export-value/required-field/lab-column/merged-cell/import-error/subcontract-two-row-template/subcontract-fixed-row-import/audit-data-start/audit-date-format/audit-plain-worksheet/user-data-start/user-date-format/invalid-user-import-report/chat-gdpr-data-start/chat-gdpr-date-format flows, including remaining NPOI formatting quirks outside the copied ability, subcontract template/import row shape, audit/entity, user, invalid-user, and GDPR chat exports.
- Production-exact ability editor subtleties beyond copied Word-paste artifact cleanup, safe UEditor alignment/color style preservation and toolbar controls, common advanced toolbar actions, safe VML image conversion, heading conversion, and local Word-image batch replacement, such as the full UEditor plugin set and deeper cross-field validation.
- Production database model and repository-backed services beyond the copied SQL Server persistence layer.

## Remaining Platform Gaps

- Live Stripe/PayPal SDK checkout, production webhook reconciliation, and real payment provider credentials.
- Production SignalR realtime delivery, cross-tab presence, and WebSocket scale-out for chat.
- Production authentication hardening beyond the local account replica: OAuth/OpenID provider integration, real IdentityServer authorization/consent grant persistence, signing-key rotation, persistent refresh-token revocation store, real SMS/email delivery, production two-factor providers, security-stamp invalidation, tenant-specific auth policy enforcement, and audit-grade impersonation controls.
- Production SMTP delivery, production-grade tenant branding binary storage/CDN behavior, live external login provider integration, tenant-isolated databases, and production multi-tenant migrations.

## Verification Notes

- `npm run build` passes for the frontend.
- `mvn test` passes for the backend.
- Browser verification covered login, dashboard, host dashboard, tenant dashboard, ability list with `GetMyOrgSetting` organization tabs/current-tab rows/current-tab action buttons, original server-side page 2 request body (`skipCount=10`, `maxResultCount=10`), original operation dropdown, row history modal, favorite/unfavorite toggle with restored final state, original `Instruction` ability-description display/edit/save/restore flow, TipTap rich-text toolbar/content editing with save/restore validation, rich-text image upload/insert flow, rich HTML description rendering with script/event stripping, organization-configured table columns, property-driven ability edit fields, front-end create required-field validation without console errors, original-style ability import preview instructions plus enabled save-new/save-new-plus-duplicate buttons on a valid double-header Excel preview, and lab/qualification search controls, ability query as a query-only user with `Ability.Description` HTML display and query-response lab dropdown, ability query server-side page 2 request body (`skipCount=10`, `maxResultCount=10`), ability query row-scoped history for `全铁含量`, ability query lab/qualification search with `TJ + ALL` returning the expected row, tabbed ability editor, favorites, subcontract ability with filtered server-side page 2 request body (`filter`, `skipCount=10`, `maxResultCount=10`), standard update page, roles, users with export/import/permissions/unlock/reset-password actions and permission modal, organization unit pages with member/role counts and add-role modal, the profile/avatar/security/delegation tabs on desktop and mobile widths, responsive sidebar collapse without horizontal overflow, the account security page token refresh, external login card, session sign-in token refresh UI, tenant availability flow, linked account table, recent linked accounts, recent login attempts, and link action, the language/text management page, the notification center, cache management with active nested menu expansion, the dynamic parameter tabs plus allowed input type/entity dropdowns, the Host/Tenant settings tabs, the UI customization page/menu entry, the Webhook subscription/send-attempt tabs, the chat page/menu/header/send controls, the tenant/edition/subscription page tabs plus payment creation action, the ability history page with property detail modal, the audit log/entity history tabs with property and instance-history modals plus export download, and the Web log page refresh plus `WebSiteLogs.zip` download.
- API verification covered ability list/edit payloads with expanded fields, ability create required-field failures, duplicate ability rejection, ability dynamic filtering through both replica `filterItems` and original ABP `filter` array payloads, `FindPageAblibities` and `FindAblibities` `skipCount/maxResultCount/totalCount` pagination contracts, ability `isCollected` status across `MyFavorite/AddItem` and `RemoveItem`, query rows not leaking management-page `isCollected`, lab/qualification filtering (`TJ + ALL`, `SH + CMA`), query-user `FindAblibities` access with `labs` response data and no laboratory-management permission, row-scoped `AbilityQuery/FindHistory`, `AbilityHistory/GetAbilityHistory` list rows with ids, `AbilityHistory/GetHistoryDetail` property rows, Session client-visible `Ability.Description`, HTML description sanitization helper, rich-text editor normalization helper, UEditor-compatible image upload and inline image read URL, `EditDesc` denial for query-user ability-description updates, ability create persistence across backend restart, ability type lookup, current organization ability settings including original `[Description]`-attributed `AbilityTable` property list/order, original `AbilityPropertyDto.Name`/`CamelCase` output, and PascalCase `PropertyName` save/read compatibility, original two-row ability template download with merged laboratory-capability header, org-scoped template download using `orgId` and `propertyName` column filtering, stable lab column order, ability export download with org-scoped file/sheet names, original two-row headers, bold 12pt header style, original default column widths, `CHEM/NF/Lab Group` special columns, `CMA,CNAS` lab value order, and original `filter` request body scoping, original-template ability upload/save with `ALL` lab value mapped to CNAS+CMA, original-template ability upload reading vertically merged required cells, ability import required-field errors and error-report download, ability `DeleteAll` by organization name, subcontract `FindList` pagination payload helper, subcontract template download, subcontract upload preview/save, subcontract import error-report download, sample type create/edit/list-by-org/delete, sample create/edit/filter/delete, lab edit lookup, standard update upload/preview/apply/rollback, standard update missing-number error-report download, Web Host MVC redirects, anti-forgery token cookie/header, MVC profile-picture upload output, chat upload/download, binary file download, role-based authorization matrix checks, signed JWT login/session, full SessionAppService bootstrap metadata, session sign-in token refresh, refresh token exchange, TokenAuth external provider list, ExternalAuthenticate access/refresh token exchange, DelegatedImpersonatedAuthenticate POST route, tenant availability states, account registration, tenant self-registration trial/paid/duplicate/subscription-start guard flows, Install appsettings/database/setup/admin-password rollback, password reset and follow-up login, email activation, local two-factor code, impersonation authenticate, linked-account authenticate, UserLink link/list/recent/unlink/relink, user export/download including original `UserList.xlsx` columns, blank second row, third-row data start, and creation-date cell format, user import valid row creation, invalid row rejection, `InvalidUserImportList.xlsx` download with original sheet/header/data-start layout, original invalid-user import notification data, original success import notification, user-specific permissions update/reset, user unlock, user password reset and follow-up login, organization unit code/counts, organization FindUsers/FindRoles, add/remove role flow, recent login attempts with success and failure rows, host dashboard top stats/recent tenants/expiring tenants/income statistics/edition statistics, tenant dashboard data/top stats/sales summary/member activity/regional stats/general stats, dashboard customization denial/default layout/definitions/page/widget mutations, AuditLog list/export/entity object types/entity changes/entity type changes/entity property changes/entity export including original file names, headers, column order, blank second row, third-row data start, and Excel date-time cell formats, ability history detail, WebLog latest/download, CommonLookup edition combobox/user lookup/default edition, Timing timezone list/combobox selected state, Demo UI permission denial, echo/lookups, file upload, and 1MB size-limit error, password verification/change/rollback, profile update/revert, profile picture update/read, Google Authenticator update/disable, SMS verification reject/accept, profile language change, collected-data zip preparation with `App.GdprDataPrepared` notification data, binary download, `ProfileInfo.txt`, `ProfilePicture.png`, and original Chat Excel headers, blank second row, third-row data start, and date-time cell format, user delegation create/list/delete, language permissions, language CRUD, language text updates, notification list/filter/unread count, notification settings update/revert, mark-read behavior, cache permission denial, cache list, single cache clear, clear-all behavior, dynamic parameter permission denial, dynamic parameter CRUD, dynamic parameter value CRUD, entity dynamic parameter CRUD, entity dynamic parameter value CRUD, dynamic parameter definition input type/entity lookups, bulk save, clean behavior, Host/Tenant settings permission denial, host setting update, tenant setting update, ability setting update, test email simulation, tenant logo/CSS upload/read/session metadata/clear, UI customization permission denial, theme list, setting update, default update, theme activation, system default restore, Webhook permission denial, available Webhooks, subscription add/update/activate, test publishing, send attempts, event detail, resend behavior, Chat/Friendship permission denial, friend list, message history, mark-read, send message, add friend by username, username friendship-request unknown-tenant error, username friendship-request missing-user error, duplicate-friend guard by id and username, self-friend guard by id and username, block/unblock/accept missing-friendship errors, block, unblock, Tenant/Edition/Payment permission denial, tenant list, edition list, tenant feature update/reset, active payment gateways, payment creation, Stripe session creation/confirmation, payment completion, invoice creation, invoice lookup, and Stripe/PayPal configuration.
- Web Host MVC API verification now also covers original `GET /Ui/Login` return-url preservation for the React login page, original `POST /Ui/Login` form-login redirect behavior, local `Consent/Index` GET view-model construction, POST accepted-scope redirect, and original missing-scope validation message, original missing-file HTTP 404 responses for `File/DownloadTempFile` and `File/DownloadBinaryFile`, original `DownloadTempFile` `FileDto` required `fileName`/`fileToken` validation plus request-file-name response headers, anonymous `Profile/GetDefaultProfilePicture` returning a PNG response, and `Profile/UploadProfilePicture` plus `Chat/UploadFile` accepting any multipart field name by reading the first uploaded file.
- Subcontract Excel/API verification now covers the original NPOI reader-compatible upload row shape and local matching template helper: merged `分包能力` title row, fixed second-row headers, third-row data start, formula-cell display values through `DataFormatter`/formula evaluation, frozen top rows, rejection of the previous single-row-header parsing behavior, original nested `FileDto` validation where submitted `file` objects require `FileName`/`FileToken` but not `FileType`, original `SaveExcelData` behavior that replaces old rows for imported lab names even when `OnlySaveNew=true`, and original `FindList` keyword search limited to `LabName`/`ContactDetails`/`TestCategory`/`CmaOrCnas`; frontend unit verification covers the matching `FileDto` required/optional type contract and original immediate subcontract upload-save request body without `OnlySaveNew`.
- Ability, ability history, audit log/entity-change, subcontract, organization lookup, user-list, tenant-list, payment-history, common user lookup, notification-list, and webhook send-attempt paging API verification now covers the original `PagedInputDto`/`PagedAndFilteredInputDto`/`PagedAndSortedInputDto`/`FindPageAblibitiesInput` ranges: `MaxResultCount` must be 1-1000 and `SkipCount` must be non-negative for `Ability/FindPageAblibities`, `AbilityQuery/FindAblibities`, `AbilityHistory/GetAbilityHistory`, `AuditLog/GetAuditLogs`, `AuditLog/GetAuditLogsToExcel`, `AuditLog/GetEntityChanges`, `AuditLog/GetEntityChangesToExcel`, `AuditLog/GetEntityTypeChanges`, `SubcontractAbility/FindList`, `OrganizationUnit/FindUsers`, `OrganizationUnit/FindRoles`, `User/GetUsers`, `Tenant/GetTenants`, `Payment/GetPaymentHistory`, `CommonLookup/FindUsers`, `Notification/GetUserNotifications`, and `WebhookSendAttempt/GetAllSendAttempts`; `User/GetUsersToExcel` remains on the original non-paged filter input.
- Ability-table MVC upload API verification now covers the original top-level response fields for `AbilityTable/UploadNewStandard`, `AbilityTable/UploadSubcontractAbility`, and `AbilityTable/UploadAbilityTable`, the original first-file multipart behavior independent of form field name, original `UploadAbilityTable` pre-parse guards for missing organization ability settings and missing organization units, ability-table `labCodeList` reflecting only lab columns present in the uploaded Excel file, and original row context where `orgName` comes from the requested organization and `typeName` comes from the worksheet name rather than uploaded `业务线/类型` columns.
- User Excel API verification now also covers original `UserList.xlsx` blank second row plus third-row data start, `GetUsersToExcelInput` blank sorting normalization to `Name,Surname`, original invalid-workbook `App.SimpleMessage` warning notification, `Users/ImportFromExcel` accepting any multipart field name by reading the first uploaded file, fixed-column import independent of header names, multi-worksheet import, blank first-column row skipping, and frontend unit verification covers the matching invalid-file import status.
- Permission API verification now covers `PermissionAppService.GetAllPermissions` access for a signed-in user-management account without `Pages.Administration.Roles`, matching the decompiled app service's lack of role-management authorization on that route, plus rejection of the old local `mock-access-token` shortcut.
- Ability write API verification now covers `Ability/CreateAbility` and `Ability/UpdateAbility` returning `result:null` while persisting created and edited ability rows.
- Delete route API verification now covers the original generated HTTP DELETE query contracts for checked app-service delete/remove routes, including side-effect checks for ability, ability bulk delete, lab, sample type, sample, and dynamic parameter deletions, plus the original `UserDelegation/RemoveDelegation` ownership error for missing delegations.
- Business GET route API verification now covers original generated GET query contracts for `Ability/GetAbilityForEdit`, `Ability/GetOrgTypeLit`, `Ability/GetTemplateExcel`, `Laboratory/GetLabForEdit`, `SampleType/GetForEdit`, `SampleType/GetListByOrg`, `Sample/GetForEdit`, and `Sample/GetList`.
- Administration GET route API verification now covers original generated GET query contracts for `Role/GetRoles`, `Role/GetRoleForEdit`, `User/GetUsers`, `User/GetUsersToExcel`, `User/GetUserForEdit`, `User/GetUserPermissionsForEdit`, `Language/GetLanguageForEdit`, `Tenant/GetTenants`, `Tenant/GetTenantForEdit`, `Tenant/GetTenantFeaturesForEdit`, `Edition/GetEditionForEdit`, and `Edition/GetTenantCount`, plus original `GetUsersInput` blank sorting normalization to `Name`, original `GetUsersToExcel` blank sorting normalization to `Name,Surname`, and original `GetTenantsInput` blank sorting normalization to `TenancyName` with the `editionDisplayName` sorting alias.
- Organization unit GET route API verification now covers original membership list sorting aliases for `GetOrganizationUnitUsers` and `GetOrganizationUnitRoles`, including `userName` and `displayName`.
- Language GET route API verification now covers original generated GET query contracts for `Language/GetLanguageTexts`, plus original `GetLanguageTextsInput` validation for non-negative paging, required `SourceName`, required 2-128 character `TargetLanguageName`, 128-character source/base language limits, and matching React query parameter mapping.
- Audit GET route API verification now covers original generated GET query contracts for `AuditLog/GetAuditLogs`, `AuditLog/GetAuditLogsToExcel`, `AuditLog/GetEntityChanges`, `AuditLog/GetEntityTypeChanges`, `AuditLog/GetEntityChangesToExcel`, and `AuditLog/GetEntityPropertyChanges`, plus ability-history explicit `changeTime ASC` sorting, `AuditLogs.xlsx` blank second row, third-row data start, date-time cell format, default-width skipped autosize columns, and absence of non-original freeze panes/table auto-filters.
- Dashboard customization route API verification now covers original generated GET query contracts for `DashboardCustomization/GetUserDashboard`, `GetDashboardDefinition`, `GetAllWidgetDefinitions`, and `GetSettingName`, plus the original `Unknown Dashboard: <name>.` error for unknown user dashboards/dashboard definitions/widget definitions and layout writes (`SavePage`, `RenamePage`, `AddNewPage`, `AddWidget`, `DeletePage`), dashboard-scoped widget definition filtering, `SavePage` preservation of existing page names and missing-page failure, `AddNewPage` and `RenamePage` input name preservation including blanks, `AddWidget` input `Width`/`Height` preservation, original top-left placement with `PositionX=0` and `PositionY=max(PositionY+Height)`, missing-page failure, and default-layout restoration when the last custom page is deleted.
- Host/Tenant dashboard GET route API verification now covers original generated GET query contracts for `HostDashboard/GetTopStatsData`, `HostDashboard/GetIncomeStatistics`, `HostDashboard/GetEditionTenantStatistics`, `TenantDashboard/GetDashboardData`, and `TenantDashboard/GetSalesSummary`, including the original fixed `DashboardPlaceholder1=125` and `DashboardPlaceholder2=830` values, original status `2` payment filtering, expiring-tenant final remaining-day/tenant-name ordering and rounded-total-days remaining count, edition-stat exclusion of tenants without assigned editions, `IncomeStastistic.Date` date-time output, `Label` short-date formatting, zh-Hans week-start splitting, calendar-month grouping, and tenant dashboard random-generator labels/ranges for sales summaries, daily sales, profit shares, member activities, and regional stats; frontend unit verification covers the matching query URL builders and original parameter casing.
- Ability history GET route API verification now covers original generated GET query contracts for `AbilityHistory/GetAbilityHistory` and `AbilityHistory/GetHistoryDetail`.
- Organization unit GET route API verification now covers original generated GET query contracts for `OrganizationUnit/GetOrganizationUnitUsers` and `OrganizationUnit/GetOrganizationUnitRoles`, plus original positive `Id`, `SkipCount>=0`, `MaxResultCount` 1-1000 validation, and matching React required-id API typing.
- Organization unit edit API verification now covers original `CreateOrganizationUnitInput` / `UpdateOrganizationUnitInput` / `MoveOrganizationUnitInput` validation for required `DisplayName`, 128-character maximum, positive update/move `Id`, plus `UsersToOrganizationUnitInput` / `RolesToOrganizationUnitInput` / `UserToOrganizationUnitInput` / `RoleToOrganizationUnitInput` positive id validation for add/remove membership writes with matching React organization-unit page/API guards.
- Account relation GET route API verification now covers original generated GET query contracts for `UserDelegation/GetDelegatedUsers` and `UserLink/GetLinkedUsers`, including original `MaxResultCount`/`SkipCount` range validation for both GET-query and POST-body inputs plus original user-delegation username sorting.
- Account guard API verification now covers the original `UserLink/LinkToUser` required username/password validation with matching frontend API typing, same-account and must-change-password errors, `UserLink/UnlinkUser` no-linked-account error, `User/DeleteUser` current-user delete error, plus `Account/Impersonate` and `Account/SwitchToLinkedAccount` positive user-id validation with matching frontend API typing.
- Profile picture GET route API verification now covers original generated GET query contracts for `Profile/GetProfilePictureById` and `Profile/GetFriendProfilePictureById`, plus anonymous `Profile/GetProfilePictureById` access.
- Common lookup/timing API verification now covers original generated GET query contracts for `CommonLookup/GetEditionsForCombobox`, original `CommonLookup/FindUsers` filter scope plus paging validation, `Timing/GetTimezones`, and `Timing/GetTimezoneComboboxItems`.
- Ability-property and edition-combobox GET route API verification now covers original generated GET query contracts for `AbilityProperty/GetOrgAbilitySetting` and `Edition/GetEditionComboboxItems`; frontend unit verification covers the matching query URL builders.
- POST-without-body route API verification now covers original generated contracts for `Account/BackToImpersonator`, `Ability/FindAllAblibities`, `AbilityProperty/AbilityPropertyList`, `Dashboard/Statistics`, `Dashboard/OrgCount`, `Dashboard/ChangeCountInWeek`, `Install/CheckDatabase`, `Laboratory/List`, and `WebLog/DownloadWebLogs`; frontend unit verification covers the matching API-layer method choices.
- Install API verification now covers original `InstallDto` required validation for `ConnectionString`, `AdminPassword`, `WebSiteUrl`, and `DefaultLanguage` before setup state is changed, plus original single `WebSiteRootAddress` appsettings output when `serverUrl` is blank; frontend unit verification covers the matching required `InstallDto` fields.
- Back-to-impersonator API verification now covers an impersonated tenant-2 admin session returning from a tenant-1 target user back to the original tenant-2 impersonator, plus the same return path after switching to a linked account while impersonated, including returned `tenancyName` and final session tenant, and delegated-session rejection after the backing delegation is removed.
- GraphQL API verification now covers `/graphql` role, organization-unit, and user queries, POST JSON and GET query-string transport, `variables`, `operationName`, top-level and nested field aliases, minimal `__schema` / `__type` introspection, original field-level permission errors, uppercase `MaxResultCount`, nested user roles/organization units, recursive selection-set field trimming, `/ui/playground`, and the original behavior where nested user details do not require separate role or organization-unit permissions.
- PUT update route API verification now covers original generated update contracts for ability, organization unit, user permissions, profile/session, language text, dynamic parameters, webhook subscription, tenant, tenant features, and edition routes; frontend unit verification covers the matching `apiPut` method choices.
- Demo UI helper query route API verification now covers original generated query contracts for `DemoUiComponents/SendAndGetDate`, `SendAndGetDateTime`, `SendAndGetDateRange`, `SendAndGetValue`, and `GetCountries`, plus `UploadFiles` returning BinaryObject GUID ids that are readable through `File/DownloadBinaryFile`; frontend unit verification covers the matching query URL builders.
- Payment GET/query route API verification now covers original generated query contracts for `Payment/GetPaymentInfo`, `Payment/GetPaymentHistory`, `Payment/GetActiveGateways`, `Payment/GetPayment`, `Payment/GetLastCompletedPayment`, `Payment/HasAnyPayment`, `Payment/BuyNowSucceed`, `Payment/NewRegistrationSucceed`, `Payment/UpgradeSucceed`, `Payment/ExtendSucceed`, `Payment/PaymentFailed`, `Payment/SwitchBetweenFreeEditions`, `Payment/UpgradeSubscriptionCostsLessThenMinAmount`, `StripePayment/GetPayment`, `StripePayment/GetPaymentResult`, and `PayPalPayment/ConfirmPayment`; frontend unit verification covers the matching query URL builders.
- Missing original route API verification now covers frontend exposure for `Ability/GetAllUnits`, `Account/ResolveTenantId`, `Account/DelegatedImpersonate`, `OrganizationUnit/MoveOrganizationUnit`, `TokenAuth/TestNotification`, TokenAuth `LogOut` GET, TokenAuth `Authenticate` Base64 password handling with matching frontend payload encoding, original `UserNameOrEmailAddress`/`Password` DTO validation, original password-reset challenge output, and original single-sign-in return URL parameters, TokenAuth `SendTwoFactorAuthCode` pending-challenge success and missing-challenge failure plus original `UserId`/`Provider` DTO validation, TokenAuth invalid two-factor code failure, TokenAuth remember-client token timing after two-factor verification, TokenAuth `GetExternalAuthenticationProviders` blank-setting and configured-provider behavior, TokenAuth `ExternalAuthenticate` required/length DTO validation for provider payloads, TokenAuth `ImpersonatedAuthenticate` / `LinkedAccountAuthenticate` POST-query calls, and the three dynamic Payment success callbacks, plus backend `TokenAuth/TestNotification` notification creation, `Account/Impersonate` / `Account/SwitchToLinkedAccount` original positive user-id validation, `Account/DelegatedImpersonate` returning the tenancy name from the saved delegation tenant, original TokenAuth method aliases, and stale delegated tokens failing subsequent Session calls with `ThereIsNoActiveUserDelegationBetweenYourUserAndCurrentUser`.
- Account resolve-tenant API verification now covers the original `AccountAppService.ResolveTenantId` blank-`c` behavior where a logged-in request returns the current session tenant id.
- Session tenant-context API verification now covers a tenant-2 impersonated authenticate flow where `GetCurrentLoginInformations.tenant` and `UpdateUserSignInToken.encodedTenantId` both reflect the token tenant instead of the default tenant.
- Tenant branding API/frontend verification now covers a tenant-2 token uploading, reading, and clearing Logo/CSS without overwriting tenant 1 resources, `TenantCustomization/UploadLogo` and `UploadCustomCss` accepting any multipart field name by reading the first uploaded file, plus React settings URL construction from the current session tenant instead of hardcoded `tenantId=1`.
- Tenant settings API verification now covers tenant-1 and tenant-2 tokens saving and reading different `TenantSettings/GetAllSettings` email/billing values without overwriting each other.
- Original frontend route alias verification now covers React route exposure for `/passport/login`, `/ablibity/subcontract-ability`, `/sys/auditlogs`, exception pages `/exception/403`, `/exception/404`, `/exception/500`, and `/exception/trigger`, plus backend MVC `/Error` status dispatch to those copied exception pages and menu exposure for original `app-data.json` links `/ablibity/subcontract-ability` and `/logs/audit_log`.
- Original NG-Alain template route verification now covers React route-table exposure for the checked `widgets`, `style`, `extras`, `pro`, and `delon` deep links, shared `TemplateDemoPage` mounting through React Router, and the original default child redirects for `/pro/account/center` and `/pro/account/settings`.
- Ability-description frontend unit verification now covers UEditor-style Word-paste cleanup for conditional comments, `Mso*` classes/styles, namespaced Word tags, preserved emphasis, preserved table content and safe table layout attributes, safe alignment/text-color/background-color style preservation, dangerous CSS stripping, safe VML shape image conversion, `MsoHeading` title conversion, local `file://` Word-image pending placeholders with `word_img` markers, replacement of those placeholders with uploaded UEditor image URLs, and copied block-format/alignment/text-color/background-color options for the richer editor toolbar.
- Ability query frontend unit verification now covers the original query search fields and copied query table columns, including price, method English description, cycle, sample mass, particle size, remark, laboratory ability, history action, no management-page `typeName`/`isCollected` columns, and the original semicolon-joined query laboratory ability text.
- Ability query API verification now covers `AbilityQuery/FindAblibities` lab/qualification filtering where a laboratory ability saved with both `hasCnas` and `hasCma` is returned by both `CNAS` and `CMA` qualification filters.
- Webhook query route API verification now covers original generated query contracts for `WebhookSubscription/GetSubscription`, `WebhookSubscription/IsSubscribed`, `WebhookSubscription/GetAllSubscriptionsIfFeaturesGranted`, `WebhookSendAttempt/GetAllSendAttempts`, `WebhookSendAttempt/GetAllSendAttemptsOfWebhookEvent`, and `WebhookSendAttempt/Resend`.
- Detail/selection GET route API verification now covers original generated query contracts for `MyFavorite/GetMyFavoriteAbilityList`, `MyFavorite/GetMyFavoriteForEdit`, and `Invoice/GetInvoiceInfo`; frontend unit verification covers those plus `WebhookEvent/Get` and `TenantRegistration/GetEdition` query URL builders.
- Tenant registration API verification now covers `GetEditionsForSelect` logged-in upgrade selection: current, cheaper, and equal-priced editions are hidden after a completed payment, remaining upgrade editions preserve monthly-price order, and `GetEdition` returns the original empty `additionalData` dictionary.
- Tenant registration API verification now covers original host registration settings: provider defaults, enabled captcha empty-response rejection without creating a tenant, disabled self-registration rejection without creating a tenant, required admin-email validation without creating a tenant or inventing a local placeholder address, original `RegisterTenantInput` `TenancyName`/`Name` 64-character, `AdminEmailAddress` 256-character email, and optional `AdminPassword` 32-character validation, default-active handling for free registrations, and `isEmailConfirmationRequired` output. Frontend unit verification covers the matching required-field API type.
- Account registration API verification now covers original tenant user-registration settings: provider defaults, enabled captcha empty-response rejection without creating a user, disabled self-registration rejection without creating a user, required-field and length-limit validation without creating incomplete users, React registration-form field caps for the same `RegisterInput` limits, default-active handling, `canLogin` calculation with email-confirmation requirements, token-tenant setting selection for impersonated registration requests, `SendPasswordResetCode`/`SendEmailActivationLink` unknown-email `InvalidEmailAddress` failures, `SendPasswordResetCodeInput` required and max-256 email validation before issuing reset codes with matching React password-reset email caps, `SendEmailActivationLinkInput` required email validation with matching frontend required-email API typing, `ResetPassword` invalid-code `InvalidPasswordResetCode` failures, `ResetPasswordInput.c` and `ActivateEmailInput.c` ABP encrypted payload resolution, `ResetPassword`/`ActivateEmail` rejecting the non-original local fixed code when the stored code differs, `ActivateEmail` invalid-code `InvalidEmailConfirmationCode` failures, already-confirmed `ActivateEmail` success without code rechecking, `IsTenantAvailableInput` required and max-64 validation with matching React tenant-name cap, `IsTenantAvailable` inactive responses with `tenantId:null`, and active tenant server-root output using the configured address instead of inventing a tenant subdomain.
- Notification/chat query route API verification now covers original generated contracts for `Notification/GetUserNotifications`, `Notification/DeleteAllUserNotifications`, `Notification/UpdateNotificationSettings`, and `Chat/GetUserChatMessages`, including notification `StartDate`/`EndDate` list/unread/delete filtering, original notification paging validation, original notification settings DTO shape, `SetNotificationAsRead` and `DeleteNotification` ownership errors, original positive `GetUserChatMessagesInput.UserId` validation, older-message `MinMessageId` cursor behavior, missing-target `Chat/SendMessage` returning `Target user could not be found. It's probably deleted.`, sender-blocked `Chat/SendMessage` returning `User is blocked.`, and receiver-blocked sends skipping the receiver-side message copy; frontend unit verification covers the matching query URL builders and required user-id service typing.
- Friendship API verification now covers original positive `UserId` validation for `CreateFriendshipRequest`, `BlockUser`, `UnblockUser`, and `AcceptFriendshipRequest`, original self-friend failures for both `CreateFriendshipRequest` and `CreateFriendshipRequestByUserName`, returning `You can not be a friend with yourself.`, username-request missing-user failures returning `There is no tenant defined with name .` like the original `GetUserIdentifier`, and block/unblock/accept missing-friendship failures returning `Friendship does not exist between ...`; frontend unit verification covers required user-id service typing for the user-id friendship routes.
- Dynamic-parameter query route API verification now covers original generated contracts for `DynamicParameter/Get`, `DynamicParameter/FindAllowedInputType`, `DynamicParameterValue/Get`, `DynamicParameterValue/GetAllValuesOfDynamicParameter`, `EntityDynamicParameter/Get`, `EntityDynamicParameter/GetAllParametersOfAnEntity`, `EntityDynamicParameterValue/Get`, `EntityDynamicParameterValue/GetAll`, and `EntityDynamicParameterValue/GetAllEntityDynamicParameterValues`, including original required `EntityFullName`/`EntityId` validation plus the original output-item shape with input type metadata, selected values, and allowed dynamic-parameter values; frontend unit verification covers the matching query URL builders and required paired parameter type.
- Settings/UI method route API verification now covers original generated HTTP `PUT` contracts for `HostSettings/UpdateAllSettings`, `HostSettings/UpdateAbilitySettings`, `TenantSettings/UpdateAllSettings`, `UiCustomizationSettings/UpdateUiManagementSettings`, and `UiCustomizationSettings/UpdateDefaultUiManagementSettings`, plus `UiCustomizationSettings/ChangeThemeWithDefaultValues?themeName=...` as POST-query; frontend unit verification covers the matching theme-change query URL builder.
- Favorite API verification now covers `MyFavorite/GetMyFavoriteList` prepending the original `id:null` / `默认清单` item and returning only the original `id/name` DTO fields, current-user scoped custom groups/default items, default-list `MyFavoriteId:null` add/list behavior without creating a fake `默认收藏` group, `MyFavorite/SaveOrUpdateMyFavorite` returning `result:null` while creating and updating favorite groups, plus the original duplicate-name error `名称已存在`; ability-list verification covers current-user scoped `isCollected`, and frontend unit verification covers read-only default-list selection state and removal of the non-original ability-count dependency.
- Ability property API verification now covers `AbilityProperty/SaveOrgSetting` returning `result:null` while persisting enabled fields, labs, public state, and description.
- Ability property API verification now also covers the exact original `[Description]`-attributed `AbilityTable` property list/order, original PascalCase field names in `AbilityPropertyList`, `camelCase` DTO values, enabled-state matching against PascalCase saved settings, camelCase `GetOrgAbilitySetting` output, and PascalCase-driven ability template column filtering.
- Standard update API verification now covers `AbilityTable/UploadNewStandard` row fields as original `old/new/name/statu/remark`, `Standard/UploadNewStandard` accepting the original `new` JSON field, returning `result:null`, applying standard-number replacements including blank new values, plus the original History source-file copy named as `currentUser$18-digit FILETIME$originalFileName`.
- User edit API verification now covers original `GetUserForEdit` create defaults: `IsActive=true`, `Password=qazwsxEDCRFV`, false security toggles for change-password, two-factor, and lockout, plus the original `UserRoleDto` role fields, default role `isAssigned` state, `CreateOrUpdateUser` returning `result:null` after saving user role assignments while using the submitted new-user password, original `UserEditDto.Normalize()` surname value `-`, required-field/email validation, original `AssignedRoleNames` required validation, and original `UserEditDto` string-length validation including the optional `Surname` 64-character cap before creating users, `UpdateUserPermissionsInput` positive `Id` range and required `GrantedPermissionNames` validation with matching frontend required-id API typing, `setRandomPassword` generating a password from current tenant password-complexity settings instead of saving the submitted password, the React user modal exposing that random-password switch and copied field caps including surname, and `ResetUserPassword` resetting to `qazwsxEDCRFV` without forcing a password-change challenge.
- Role edit API verification now covers original `GetRoleForEdit` create-mode role object, empty granted permission names, `FlatPermissionDto` fields including `isGrantedByDefault`, permission ordering by `DisplayName`, `CreateOrUpdateRole` returning `result:null` after saving role permissions, and original `CreateOrUpdateRoleInput` required-field validation for `Role`, `GrantedPermissionNames`, and `Role.DisplayName`.
- Language API verification now covers `GetLanguages` display-name sorting, `GetLanguageForEdit` language-name/flag comboboxes and selected states, `CreateOrUpdateLanguage` culture-derived display names plus original `IsEnabled` mapping, `CreateOrUpdateLanguage` and `UpdateLanguageText` returning `result:null` while persisting the language row and localized text value, plus the original duplicate-language `This language already exists!` and tenant-side `Tenants cannot create language.` errors. It also covers the original language DTO validation surface: required wrapper `Language`, rejection of top-level-only language fields, `Name`/`Icon` 128-character caps, `SetDefaultLanguageInput.Name` required/max 128, required `LanguageName`/`SourceName`/`Key`/value for text updates, and 128/128/256-character caps for those text fields; frontend unit checks cover the matching Ant Design language/icon selects, original `isEnabled` switch, input limits, and original `value` request field.
- Dynamic parameter API verification now covers `DynamicParameter/Add`, `DynamicParameter/Update`, `DynamicParameterValue/Add`, and `DynamicParameterValue/Update` returning `result:null` while persisting parameter definitions and allowed values, plus `DynamicParameter/Delete?id=...` and `DynamicParameterValue/Delete?id=...` using the original HTTP DELETE proxy contract.
- Entity dynamic parameter API verification now covers `EntityDynamicParameter/Add`, `EntityDynamicParameter/Update`, `EntityDynamicParameterValue/Add`, `EntityDynamicParameterValue/Update`, `EntityDynamicParameterValue/InsertOrUpdateAllValues`, and `EntityDynamicParameterValue/CleanValues` returning `result:null` while persisting entity mappings and entity-scoped values, including original `items` batch DTO replacement semantics and mapping/entity-scoped clean behavior, plus `EntityDynamicParameter/Delete?id=...` and `EntityDynamicParameterValue/Delete?id=...` using the original HTTP DELETE proxy contract.
- Tenant/Edition API verification now covers `Tenant/CreateTenant`, `Tenant/UpdateTenant`, `Edition/CreateEdition`, and `Edition/UpdateEdition` returning `result:null` while persisting tenant and edition changes, plus `Tenant/CreateTenant` rejecting missing admin email instead of inventing a local placeholder address, enforcing original `CreateTenantInput` tenancy-name regex and field lengths, `Tenant/UpdateTenant` enforcing original required `TenancyName`/`Name` plus 64/128-character limits, `Tenant/UpdateTenantFeatures` enforcing original positive `Id` range and required `FeatureValues` list with matching frontend required-id service typing, `Edition/CreateEdition` and `Edition/UpdateEdition` enforcing original required `Edition`, `FeatureValues`, and `DisplayName` fields, `Edition/MoveTenantsToAnotherEdition` enforcing original positive source/target edition id ranges, frontend tenant-form caps for tenancy name, name, admin email, and connection string, and frontend edition-move service typing for required ids.
- Webhook subscription API verification now covers `WebhookSubscription/AddSubscription` and `WebhookSubscription/UpdateSubscription` returning `result:null` while persisting subscription URI, activity state, subscribed events, and headers.
- Settings API verification now covers `HostSettings/UpdateAllSettings`, `HostSettings/UpdateAbilitySettings`, and `TenantSettings/UpdateAllSettings` returning `result:null` while persisting host email/billing, ability description, and tenant email/billing settings, original `HostSettingsEditDto` required sections (`General`, `UserManagement`, `Email`, `TenantManagement`, `Security`), original `TenantSettingsEditDto` required `UserManagement`/`Security` plus `ValidateHostSettings` email validation with matching frontend settings types, original `SessionTimeOutSettingsEditDto` defaults (`30`/`30`) and minimum-10 range validation with matching frontend numeric minimums, plus host/tenant `SendTestEmailInput.EmailAddress` required/max-256 validation and matching frontend input caps.
- Profile API verification now covers `Profile/UpdateCurrentUserProfile` returning `result:null` while persisting name, surname, email, phone, and English name fields, rejecting original `CurrentUserProfileEditDto` required-field and string-length violations before saving, React profile-form field caps for the same DTO limits, `ChangeLanguage` rejecting missing `ChangeUserLanguageDto.LanguageName`, `ChangePasswordInput` required `CurrentPassword`/`NewPassword` validation, `ChangePassword` returning the original `Incorrect password.` message for a wrong current password and rejecting new passwords below the tenant required length or missing required character categories, `GetPasswordComplexitySetting` anonymous access plus tenant security setting reads, `UpdateGoogleAuthenticatorKey` using the original lowercase GUID-derived 10-character key shape, plus the original `VerifySmsCode` wrong-code failure message after a verification SMS is requested, `SendVerificationSms` storing random 6-digit codes and deferring profile phone-number changes until the code is verified, frontend removal of fixed SMS-code exposure, and `UpdateProfilePicture` required/max-400 `FileToken` validation plus missing temp-file token failure.
- User delegation API verification now covers `UserDelegation/DelegateNewUser` returning `result:null` while saving the delegated-user row and current tenant for later list retrieval/delegated impersonation, deleting a delegation invalidating an existing delegated session, plus original `CreateUserDelegationDto` validation for required/positive `TargetUserId`, required `StartTime`/`EndTime`, the original start-after-end error message, matching frontend required payload/date-range checks, and the original self-delegation and source-user-only removal error messages.
- Laboratory API verification now covers `Laboratory/CreateOrUpdate` returning the original saved `Guid` string while persisting the laboratory row for `Laboratory/List`, plus original `LaboratoryDto.Code` and `LaboratoryDto.Name` required validation with matching frontend form/type verification.
- Business create/update API verification now covers original duplicate validation messages for laboratory code, sample type name, and sample name.
- Notification settings API verification now covers `Notification/GetNotificationSettings` returning only `receiveNotifications` plus `notifications`, `Notification/UpdateNotificationSettings` returning `result:null`, persistence of the receive-notifications flag plus per-notification subscription state, and original `NotificationSubscriptionDto.Name` required/max-96 validation with matching frontend hidden-name input cap.
- UI customization API verification now covers `UiCustomizationSettings/UpdateUiManagementSettings` and `UpdateDefaultUiManagementSettings` returning `result:null` while persisting active/default theme layout, menu, skin, and footer settings.
- Invoice API verification now covers `Invoice/CreateInvoice` returning `result:null` while creating a readable invoice retrievable through `Invoice/GetInvoiceInfo` with invoice number, amount, edition, and payment id, plus the original duplicate-invoice `Invoice is already generated for this payment.` error, missing invoice settings `Invoice info is missing or not completed` error, no-invoice `There is no invoice for this payment !` read error, and cross-tenant `This invoice is not yours!` ownership error.
- Payment API verification now covers `Payment/UpgradeSubscriptionCostsLessThenMinAmount` accepting the original `editionId` query/body parameter, returning `result:null`, and updating the current token tenant edition/display name. `Payment/GetPaymentInfo` now verifies both the original `Tenant edition is not assigned` failure when the current tenant has no edition and the original current-token tenant lookup. `Payment/GetPaymentHistory`, `Payment/GetLastCompletedPayment`, and `Payment/HasAnyPayment` now verify current-token tenant filtering; `GetPaymentHistory` also verifies the original `editionDisplayName` sorting alias. `HasAnyPayment` also verifies the decompiled completed-only check, where only status `Completed(5)` payments count. `Payment/CreatePayment` now verifies the original no-edition upgrade failure, current token tenant id persistence, and recurring creation changing that tenant's `SubscriptionPaymentType` to `RecurringAutomatic(1)`. `Payment/GetActiveGateways` now verifies configured `IsActive` filtering, the original default active PayPal/Stripe flags with blank credentials, and the original `PaymentGatewayModel` shape without a local `name` field, while frontend unit verification covers deriving PayPal/Stripe labels from `gatewayType`. `SwitchBetweenFreeEditions` now verifies the original tenant-no-edition, current-paid-edition, target-paid-edition failures, and current-token tenant update while keeping free-to-free switching separate from the low-amount paid upgrade path. PayPal/Stripe confirmations now verify the original `SetAsPaid()` behavior without tenant subscription updates, including leaving already completed payments completed; payment success callbacks verify the original `Paid(2)` prerequisite before transitioning to `Completed(5)` and updating the payment tenant, and cancellation verification covers `SetAsCancelled()` only cancelling `NotPaid(1)` payments. Stripe/PayPal verification now covers `GetConfiguration` returning configured gateway values instead of hardcoded local credentials, `StripePayment/GetPayment` rejecting unknown session ids, `StripePayment/GetPaymentResult` rejecting payments without stored Stripe session data and returning done only for `Completed(5)`, and Web.Host `POST /Stripe/WebHooks` returning HTTP 400 for unsigned callbacks instead of falling through to 404.
- Subscription API verification now covers the original tenant-scoped `DisableRecurringPayments` and `EnableRecurringPayments` transitions: only the current token tenant is changed, automatic recurring payment type `1` becomes manual recurring type `2`, enabling changes type `2` back to `1`, clears that tenant's `SubscriptionEndDateUtc`, and session bootstrap returns the tenant's real `subscriptionPaymentType`.
- Edition API verification now covers the original `CreateEdition` paid-expiring-edition guard, `DeleteEdition` subscribed-tenant guard, four-price `SubscribableEdition.IsFree` behavior, and the `GetEditionComboboxItems` default not-assigned item with nullable `isFree` plus `isSelected` state.
- The latest AppService method coverage audit found all 271 decompiled Shared and Application `I*AppService` method names represented by backend `/api/services/app/...` routes, React API/request-contract source, backend tests, and frontend contract tests (`backendRouteMissingCount=0`, `frontendSourceMissingCount=0`, `backendTestMissingCount=0`, `frontendTestMissingCount=0`). Production-exact behavior remains covered incrementally by the parity sections above.
